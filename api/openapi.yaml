openapi: 3.1.0
info:
  title: GenAI Telemetry Redactor Hosted API Placeholder
  version: 0.0.0
  description: |
    This repository currently owns a library and SDK, not a hosted HTTP API.
    This OpenAPI file is intentionally empty until a hosted adapter is approved by ADR.
servers:
  - url: https://api.example.invalid
    description: Placeholder only; no hosted API is implemented.
components:
  schemas:
    RedactionReportExample:
      type: object
      required: [redacted, counts, warnings]
      properties:
        redacted:
          type: boolean
        counts:
          type: object
          additionalProperties:
            type: integer
            minimum: 0
        warnings:
          type: array
          items:
            type: string
paths: {}
